# Implement Trie (Prefix Tree)

[LeetCode #208](https://leetcode.com/problems/implement-trie-prefix-tree/description/)

---

### Statement

A **trie** (prefix tree) is a tree data structure used for string retrieval.

Implement `Trie`:
- `Trie()` — initializes the trie.
- `void insert(String word)` — inserts `word` into the trie.
- `boolean search(String word)` — returns `true` if `word` is in the trie.
- `boolean startsWith(String prefix)` — returns `true` if any word starts with `prefix`.

**Constraints:**
- \\( 1 \le \text{word.length}, \text{prefix.length} \le 2000 \\)
- All strings consist of lowercase English letters.
- At most \\( 3 \times 10^4 \\) calls in total.

### Examples

**Example 1:**
```text
Input:  ["Trie","insert","search","search","startsWith","insert","search"]
        [[],["apple"],["apple"],["app"],["app"],["app"],["app"]]
Output: [null,null,true,false,true,null,true]
```

---

### Solution

*Not yet implemented.*
